function loadSavedLinks(){
    let linksContainer = document.getElementById("linksArea");
    let links = eval(localStorage.getItem("links"));

    console.log(links)
    console.log(links.length);
    for(let l = 0; l <= links.length-1; l++){
        
    }

}